# 🌡️ Conversor de Temperatura (Celsius → Fahrenheit)

Este projeto consiste em um **programa simples em Java** que converte temperaturas de **Celsius para Fahrenheit**, utilizando entrada via teclado e permitindo que o usuário **repita a execução** quantas vezes desejar.

---

## 📌 Objetivo da Atividade

Praticar os seguintes conceitos básicos da linguagem Java:

* Estrutura de repetição `do-while`
* Entrada de dados com a classe `Scanner`
* Uso da classe `Locale`
* Operações matemáticas
* Impressão formatada com `printf`
* Controle de fluxo com condicionais

---

## ⚙️ Como o Programa Funciona

1. O programa solicita ao usuário uma temperatura em **Celsius**.
2. Realiza a conversão para **Fahrenheit** usando a fórmula:

```
F = (9 × C) / 5 + 32
```

3. Exibe o valor convertido.
4. Pergunta se o usuário deseja repetir o programa (`s` para sim, `n` para não).
5. Caso a resposta seja `s`, o processo se repete.
6. Caso contrário, o programa é encerrado.

---

## 🧮 Fórmula Utilizada

```java
F = 9.0 * C / 5.0 + 32.0;
```

---

## ▶️ Exemplo de Execução

```
Digite a temperatura em Celsius: 30
Equivalente em Fahrenheit: 86.0
Deseja repetir o programa? (s/n)
s

Digite a temperatura em Celsius: 41
Equivalente em Fahrenheit: 105.8
Deseja repetir o programa? (s/n)
n

Programa encerrado
```

---

## 🛠️ Tecnologias Utilizadas

* **Java**
* **Eclipse IDE** (ou qualquer IDE compatível)
* **JDK 17+**

---

## 📂 Estrutura do Código

* Importação das bibliotecas `Scanner` e `Locale`
* Classe principal com método `main`
* Laço `do-while` para repetição do programa
* Fechamento do objeto `Scanner` ao final da execução

---

## 📚 Aprendizados

Com esta atividade foi possível reforçar:

* Leitura de dados do usuário
* Laços de repetição controlados por condição
* Boas práticas como fechamento do `Scanner`
* Organização básica de um programa Java

---

## ✍️ Autor

Projeto desenvolvido como **atividade prática de estudo em Java**, com foco em lógica de programação e fundamentos da linguagem.

🚀 Em constante aprendizado!
